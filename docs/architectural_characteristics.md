|品質特性  |項目               |推奨値                                     |備考                         |
|------|-----------------|----------------------------------------|---------------------------|
|互換性   |対応RDBMSバージョン     |MySQL 8.0+, PostgreSQL 14+, SQLite 3.35+|`RETURNING`等の機能境界          |
|互換性   |Driver抽象化の最小メソッド数|接続/切断/Exec/Query/メタデータ取得の5種             |将来トランザクション制御を追加            |
|信頼性   |クエリタイムアウト        |30秒                                     |サーバ側`statement_timeout`と併用 |
|信頼性   |接続タイムアウト         |10秒                                     |DSN接続時                     |
|信頼性   |接続リトライ回数         |3回                                      |指数バックオフ（1s, 2s, 4s）        |
|信頼性   |アイドル切断時の再接続      |自動1回試行                                  |失敗時は明示再接続                  |
|信頼性   |危険操作のconfirm     |`WHERE`なしUPDATE/DELETE、全DDLで必須          |confirm文字列入力（`yes`等）       |
|信頼性   |読み取り専用の実装方針      |接続プロファイルの属性として実装                        |プロファイル単位で切替                |
|セキュリティ|パスワードファイルの権限     |`0600`必須、それ以外は警告or拒否                    |`.pgpass`慣習に準拠             |
|セキュリティ|履歴ファイルの保存件数      |直近500件                                  |パスワード文字列はマスク               |
|セキュリティ|履歴の保存対象          |接続プロファイル属性で切替                           |prodはオプトイン                 |
|セキュリティ|接続文字列のマスク表示      |常時マスク                                   |画面共有対策                     |
|機能適合性 |文字エンコーディング       |UTF-8固定                                 |内部表現はUTF-8                 |
|機能適合性 |NULL表示文字         |`<NULL>`等の明示マーカー                        |空文字との区別必須                  |
|機能適合性 |数値・日時のロケール       |UTC・ISO 8601固定                          |カスタマイズ可                    |
|性能効率性 |SELECT結果の取得方式    |カーソル/ストリーミング                            |SQLは改変しない                  |
|性能効率性 |1回のfetchサイズ      |500行                                    |サーバ往復の単位                   |
|性能効率性 |クライアント側バッファ上限    |10,000行                                 |超過分は再fetch可能な設計            |
|性能効率性 |1ページの表示行数        |50行                                     |ターミナル高さで調整                 |
|性能効率性 |テーブル一覧の遅延読み込み    |スキーマ単位で初回のみ                             |TTL 5分のキャッシュ               |
|性能効率性 |起動時の初期表示までの目標    |1秒以内                                    |接続は遅延で良い                   |
|使用性   |キーバインドの応答時間      |100ms以内                                 |TUIの体感品質                   |
|使用性   |取得進捗の表示          |ステータスバーに常時                              |`fetched: N rows`等         |
|使用性   |取得中断             |Ctrl-Cで即時                               |サーバ側cancel送信               |
|使用性   |ヘルプメニュー          |`?`キーで表示                                |現在のコンテキストで使えるキーバインドとコマンドを一覧|
|使用性   |ヘルプの内容           |キー操作・コマンド・現在の画面の説明                      |lazygit準拠                  |
|使用性   |コンテキスト依存ヘルプ      |画面/ペインごとに表示内容を切替                        |テーブル一覧画面とエディタ画面で別の内容       |

品質特性の英語表記（ISO/IEC 25010）
互換性 = Compatibility / 信頼性 = Reliability / セキュリティ = Security / 機能適合性 = Functional Suitability / 性能効率性 = Performance Efficiency / 使用性 = Usability
優先順位は Compatibility → Reliability → Security → Functional Suitability → Performance Efficiency → Usability の順。​​​​​​​​​​​​​​​​