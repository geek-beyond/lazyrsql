## MVP
1st Step マイルストーンを定義。
次のステップに移行した場合、このドキュメントを更新する。

### 対象RDBMS

MySQL 1つに絞る。後から横展開する前提で設計はしておくが、最初の実装範囲は1つに限定する。

### 機能範囲

SELECT およびテーブル情報の参照に限定する。読み取り系のほうが利用機会が多いため、まずはここに絞る。

### SQLエディタ

環境変数 `$EDITOR` で指定されたエディタを起動する方式とする。lazygit と同じ流儀で、独自エディタの実装は後続マイルストーンに回す。
